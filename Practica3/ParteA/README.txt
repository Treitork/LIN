Instrucciones para el uso del script:

Se trata de un único script que consta de dos argumentos.
Para ejecutarlo, hay que usar los argumentos "prod" y "cons" junto a la orden de ejecución del script.
ejemplo:
-Primero en un terminal ejecutamos---> ./script.sh prod
-Despues ejecutamos en otro terminal---> ./script.sh cons
